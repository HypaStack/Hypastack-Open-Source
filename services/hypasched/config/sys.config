[
    {kernel, [
        {logger_level, info},
        %% Keep the distribution listener loopback-only (epmd is pinned to
        %% loopback via ERL_EPMD_ADDRESS in the image).
        {inet_dist_use_interface, {127, 0, 0, 1}}
    ]}
].
